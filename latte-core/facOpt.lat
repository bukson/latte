int fac(int n, boolean k, string s) {
	int i = n;
	int r = 1;
	while (i > 0) {
		int x = 3;
		r = r * i;
		i = i - 1;
	}
	return r;
}

int main() {
	int a = 15;
	string d = "abba";
	int b = 4;
	boolean c = (!(a < b) && (a < 2 * b)) || (!(a > b) && (a > 2 * b)) ;
//	boolean c = (!(a < b) && (a < 2 * b) && ( a < 3 * b));
	return fac(a,true, d);
}